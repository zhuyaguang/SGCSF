我们构建了一个基于 kubeedge 的天基计算平台，用于星载应用的部署与管理。
地面是 master 节点，星上是 worker 节点。
卫星一天只有5分钟左右的时间与地面 master 节点通信，其他时间都不在线。
每颗卫星上有1-3个边缘节点。每个节点就是一个node节点。

目前星地的带宽有限。导致了很多问题。组件间带宽竞争，管理面的组件的消息同步超时。
所有我们想构建一个统一的通信框架，通过统一通信框架来进行流量的治理，链路可观测，消息优先级设置来解决这些问题。

目前想设计下这个通信框架的架构。不涉及到具体的实现细节。
目前我想到了主要是 gateway 模式，地面有一个组件 ground-gateway，星上一个组件 satellite-gateway 。星间通信就是 ground-gateway 和 satellite-gateway 之间的通信。请评价这种模式的优缺点。然后找一下还有什么通信模式比较适合。
然后画出各种模式的通信架构图。要画清楚地面应用怎么和地面通信，星上应用怎么和星上通信。哪个是客户端，哪个是服务端，端口是多少。

因为组件都是部署在k8s上面，说清楚 地面 k8s 集群的消息怎么流转的。

要说清楚 当地面或者星上增加组件或者减少组件的时候，我们的通信框架要怎么感知到。


